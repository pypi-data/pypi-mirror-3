#!python
#THIS IS GNATIRAC LAUNCH SCRIPT

import gnatirac
import gnatirac.bootstrap 

import sys, os

# backup the current dir
current = os.getcwdu()

# change to gnatirac package dir
os.chdir(gnatirac.__path__[0])

# run zourite
gnatirac.bootstrap.run()

# restore the direction
os.chdir(current)