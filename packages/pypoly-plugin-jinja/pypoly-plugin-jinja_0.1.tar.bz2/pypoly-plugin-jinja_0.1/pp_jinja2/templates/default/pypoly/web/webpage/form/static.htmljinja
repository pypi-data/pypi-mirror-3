{% if element.type.is_type("form.label") %}
	<div id="{{element.id}}">{{element.value}}</div>
{% elif element.type.is_type("form.label.link") %}
	<a id="{{element.id}}" href="{{element.url}}">{{element.value}}</a>
{% endif %}
