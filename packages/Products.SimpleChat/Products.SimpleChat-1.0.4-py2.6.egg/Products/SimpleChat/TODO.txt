Fix expired username after Zope restart.

    Not a problem?  Looks like it works fine, might be storage that
    expires after an hour, adjusted to two hours.

Find a better way of banning users, if more than one are using the
same gateway (IP address) innocent users can get banned.

Fix way of translating things in Python code, why it doesn't work is
unknown.
