SCRIPT=${1:-../python/run_dataset.py}

. ./env

EXE=$PYTHON
ARGS="$SCRIPT"

GDB_CMDS=gdb-cmds.tmp
cat <<EOF >$GDB_CMDS
file $EXE
set breakpoint pending on
b stem.h:1175
# catch throw
run $ARGS STEM Tompa
EOF

gdb -x $GDB_CMDS && rm $GDB_CMDS

