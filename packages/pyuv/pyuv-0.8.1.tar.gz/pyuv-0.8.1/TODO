
- Better docstrings

- Support compiling with Visual Studio

- Find out why some tests fail on Windows
    - Multicast
    - IPC, sharing a TCP handle

- Prevent abort when signal handle is added to a non-default loop
- Add SignalChecker 'fake' handle? or Check for signals on every callback?
- Backport fixes to v0.8
    - Also backport some enhancements
    - Make a release

