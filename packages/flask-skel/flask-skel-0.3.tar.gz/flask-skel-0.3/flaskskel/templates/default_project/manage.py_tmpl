from flaskext.script import Server, Manager, Shell
from {{ flask_project_name }} import create_app

app = create_app()

manager = Manager(app)
manager.add_command("runserver", Server(host='0.0.0.0'))

#@manager.command
#def resetdb():
#    if prompt_bool("Are you sure"):
#        db.drop_all()
#        db.create_all()

def _make_context():
    return dict(app=app)
manager.add_command("shell", Shell(make_context=_make_context))

if __name__ == "__main__":
    manager.run()
