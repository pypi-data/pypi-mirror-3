#!/usr/bin/env python

if __name__ == '__main__':
    import os, sys, subprocess
    from os.path import abspath, dirname
    path = abspath(dirname(__file__))
    print os.chdir(path)
    sys.exit(subprocess.call(["make", "html"]))
