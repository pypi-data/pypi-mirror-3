[global]
# You can place test-specific configuration options here (like test db uri, etc)

# Global parameters

title = Firelet
listen_address = 0.0.0.0
listen_port = 8083
logfile = firelet.log

demo_mode = True

# this value is not used during the tests
data_dir = bogus

# Comment out to smtp_server_addr disable email delivery
email_sender = firelet@localhost.local

# Comma-space-separated list of destination email addresses
email_recipients = "root@localhost.local"
