CREATE TABLE "main"."recent_upload_dests" (
	 "id" integer NOT NULL PRIMARY KEY AUTOINCREMENT,
	 "path" text NOT NULL
);

CREATE UNIQUE INDEX "main"."recent_upload_dests_id" ON "recent_upload_dests" (id DESC);