import os
import sys

def run():
    lessbin = os.path.join(os.path.dirname(__file__),
                           'rb', 'bin', 'lessc')
    print sys.argv
    os.execv(lessbin, [less] + sys.argv[1:])

if __name__ == '__main__':
    run()
