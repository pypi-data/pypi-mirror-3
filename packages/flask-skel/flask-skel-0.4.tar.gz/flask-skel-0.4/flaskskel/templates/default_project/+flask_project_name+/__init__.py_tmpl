from flask import Flask
from blueprints import register_blue_prints

def create_app():
    # create app
    app = Flask(__name__)
    app.config.from_object('{{ flask_project_name }}.config')
    # import views
    # views are handled at blueprint level
    # blueprints
    register_blue_prints(app)
    
    return app
